# colcon build-артефакты (обычно живут в ~/ros2_ws/build|install|log, вне
# этих папок — но игнорируем защитно на случай in-source сборки/копипасты)
build/
install/
log/

# Python-кэш байткода
__pycache__/
*.pyc

# Bag-записи с этапа квалификации — большие (796МБ суммарно, два файла
# превышают жёсткий лимит GitHub 100МБ/файл), не код, не версионируем.
# Делимся отдельно (см. README.md).
hsl_description/data/

# Редакторы/ОС
.vscode/
*.swp
.DS_Store

# Служебные папки сторонних AI-ассистентов
.gigacode/
